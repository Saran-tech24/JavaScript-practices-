// sort method with reverse and includes

// let arr = [3, 1, 4, 1, 5, 9, 2, 6, 5];

// arr.sort();

// console.log(arr); // [1, 1, 2, 3, 4, 5, 5, 6, 9]

// reverse the sorted array

let arr = [1, 3, 2, 4, 5];

// arr.reverse();
// console.log(arr); // [5, 4, 2, 3, 1]

// check if array includes a value
console.log(arr.includes(11)); 